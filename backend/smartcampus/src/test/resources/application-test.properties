# MongoDB test settings
spring.data.mongodb.uri=${TEST_MONGODB_URI:mongodb://localhost:27017/smartcampus_test}
spring.data.mongodb.auto-index-creation=false

# Disable OAuth2 auto-config that requires real credentials
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration

# JWT config for tests
auth.jwt.secret=test-secret-key-for-unit-testing-only-must-be-256-bits-long
auth.jwt.expiration-seconds=86400

# Frontend URL
app.frontend.url=http://localhost:5173

# Dummy Google OAuth2 credentials (not used in unit tests)
spring.security.oauth2.client.registration.google.client-id=test-client-id
spring.security.oauth2.client.registration.google.client-secret=test-client-secret
spring.security.oauth2.client.registration.google.scope=openid,profile,email

app.seed.enabled=false
